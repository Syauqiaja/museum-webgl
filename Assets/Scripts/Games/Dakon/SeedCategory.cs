namespace Museum.Games.Dakon
{
    /// <summary>
    /// The two seed categories. Also used as a hole's type: a drop "matches" when the
    /// seed's category equals the hole's type. Physical sorting into stores is by category.
    /// </summary>
    public enum SeedCategory
    {
        Monocot,
        Dicot
    }
}
